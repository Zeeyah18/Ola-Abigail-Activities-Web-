export const sum = (num1, num2) => {
    return num1 + num2;
}

export const difference = (num1, num2) => {
    return num1 - num2;
}

export const product = (num1, num2) => {
    return num1 * num2;
}

export const qoutient = (num1, num2) => {
    return num1 / num2;
}